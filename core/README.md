# GenAI Normalizer — Module 1

Core provider-aware normalization primitives.

Includes enums, unified request/response models, interfaces, provider detection,
parser registry/factory, JSON-safe conversion utilities, and basic tests.
OpenAI streaming and non-streaming implementations are delivered in later modules.
